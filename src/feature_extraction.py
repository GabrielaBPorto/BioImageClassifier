from radiomics import featureextractor
import os

def extract_features(image_path, mask_path, output_file):
    """
    Extract features from an image using a mask.
    Args:
        image_path (str): Path to the input image.
        mask_path (str): Path to the mask image.
        output_file (str): File to save extracted features.
    """
    # Code to extract features and save them to a file

if __name__ == "__main__":
    # Code to process all images and masks
