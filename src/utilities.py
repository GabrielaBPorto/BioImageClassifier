def load_data(file_path):
    """
    Load data from a given file.
    Args:
        file_path (str): Path to the data file.
    Returns:
        Loaded data.
    """
    # Code to load and return data

# Additional utility functions as needed
